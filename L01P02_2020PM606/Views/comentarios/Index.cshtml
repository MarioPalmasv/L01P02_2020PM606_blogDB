@model IEnumerable<L01P02_2020PM606.Models.comentarios>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.publicacionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.comentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usuarioId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.publicacionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.comentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usuarioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cometarioId" class = "btn btn-success">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.cometarioId" class="btn btn-warning">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.cometarioId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
